#include "main.h"

/**
 * isdigit - function to check for a digit
 *
 * @c: parameter
 * Return: 1if c is a digit or 0 if not
 */
int isdigit(int c)
{
	if (isdigit(c))
	{
		return (1);
	}
	return (0);
}
