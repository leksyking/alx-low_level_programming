#include "main.h"
#include <stdio.h>

/**
 * main - a program thta prints its name
 * followed by a new line
 *
 * @argc: number of cdl arguments
 * @argv: array of cdl arguments
 *
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0 * argc]);
	return (0);
}
