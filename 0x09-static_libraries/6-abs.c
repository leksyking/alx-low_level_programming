#include <stdlib.h>

/**
 * _abs - function to compute the absolute value of an integer
 *
 * @j: the argument to be passed into the function
 * Return: the absolute number
 */
int _abs(int j)
{
	return (abs(j));
}
